{
  "name": "hades-solution-site",
  "version": "1.0.0",
  "description": "Hades Solutions",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"vite\" \"node server/index.js\"",
    "build": "vite build",
    "start": "node server/index.js",
    "preview": "npm run build && npm run start"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "vite": "^5.0.0"
  }
}
